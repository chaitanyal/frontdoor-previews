---
import { homeContent } from '../../lib/practice-view.mjs';
import { HOME_SECTION_IDS } from '../../lib/home-sections.mjs';
import Icon from './Icon.astro';

interface Props {
  config: Record<string, any>;
}

const { config } = Astro.props;
const { providers = [], conditions = [], conditionsIntro = '' } = config;
const content = homeContent(config);
const providerCount = providers.length;
const isSingleProvider = providers.length === 1;
const useTwoColumnDesktop = providerCount === 2 || providerCount === 4;
const multiProviderCardWidth = useTwoColumnDesktop
  ? 'w-full md:w-[calc(50%_-_0.75rem)]'
  : 'w-full md:w-[calc(50%_-_0.75rem)] xl:w-[calc(33.333%_-_1rem)]';
---

{
  providers.length > 0 && (
    <section
      id={HOME_SECTION_IDS.providers}
      class="home-providers section border-t border-slate-200/70 bg-white"
    >
      <div class="section-shell">
        <div class="max-w-2xl">
          <p class="eyebrow">{content.providerEyebrow}</p>
          <h2 class="section-title">{content.providerTitle}</h2>
          <p data-section-summary class="section-copy">{content.providerCopy}</p>
        </div>
        <div
          data-provider-count={providerCount}
          class:list={[
            'home-provider-grid',
            isSingleProvider
              ? 'mx-auto mt-10 grid max-w-4xl grid-cols-1 gap-6'
              : 'mt-10 flex flex-wrap justify-center gap-6'
          ]}
        >
          {providers.map((provider) => (
            <article
              class={`home-provider-card fade-in-up soft-card interactive-card flex cursor-pointer flex-col p-5 transition duration-200 hover:-translate-y-0.5 hover:shadow-xl active:scale-[0.995] focus:outline-none focus-visible:ring-4 focus-visible:ring-slate-300 ${
                isSingleProvider
                  ? 'md:grid md:grid-cols-[14rem_minmax(0,1fr)] md:gap-x-8 md:gap-y-5 md:p-8'
                  : `${multiProviderCardWidth} md:p-6`
              }`}
              tabindex="0"
              role="link"
              data-card-href={`./providers/${provider.slug}/`}
              aria-label={`View profile for ${provider.name}`}
            >
              <div
                class={`home-provider-intro flex items-start gap-4 ${isSingleProvider ? 'md:contents' : ''}`}
              >
                <div
                  class={`home-provider-image h-32 w-28 shrink-0 overflow-hidden rounded-2xl sm:h-36 sm:w-32 ${
                    isSingleProvider
                      ? 'md:col-start-1 md:row-span-3 md:h-72 md:w-56 md:rounded-[24px]'
                      : ''
                  }`}
                >
                  <img
                    loading="lazy"
                    src={provider.image}
                    alt={provider.imageAlt}
                    class="image-treatment h-full w-full object-cover object-top"
                  />
                </div>
                <div
                  class={`home-provider-meta min-w-0 pt-1 ${isSingleProvider ? 'md:col-start-2' : ''}`}
                >
                  <h3
                    class={`text-xl font-semibold leading-tight text-slate-950 ${
                      isSingleProvider ? 'md:text-2xl' : ''
                    }`}
                  >
                    {provider.name}
                  </h3>
                  <p class="mt-1 text-sm leading-6 text-slate-500">
                    {provider.credentials}
                  </p>
                  <span
                    class={`mt-4 inline-flex items-center gap-1.5 text-sm font-semibold text-brand-primary ${
                      isSingleProvider ? 'md:text-base' : ''
                    }`}
                    aria-hidden="true"
                  >
                    View profile
                    <Icon name="ArrowRight" class="h-4 w-4" />
                  </span>
                </div>
              </div>
              <p
                class={`home-provider-tagline mt-5 text-base leading-7 text-slate-700 ${
                  isSingleProvider
                    ? 'md:col-start-2 md:mt-0 md:text-lg md:leading-8'
                    : ''
                }`}
              >
                {provider.tagline || ''}
              </p>
              <div
                class={`home-provider-tags mt-4 flex flex-wrap gap-2 ${
                  isSingleProvider ? 'md:col-start-2 md:mt-0' : ''
                }`}
              >
                {(provider.heroTrustItems || provider.specialties || [])
                  .slice(0, 2)
                  .map((item) => (
                    <span class="badge-brand max-w-full">
                      <Icon name="CheckCircle" class="h-3.5 w-3.5 shrink-0" />
                      {item}
                    </span>
                  ))}
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  )
}
<section
  id={HOME_SECTION_IDS.conditions}
  class="home-conditions section relative overflow-hidden border-t border-white/60 bg-warm-50"
>
  <svg
    aria-hidden="true"
    class="conditions-leaf pointer-events-none absolute right-4 top-8 h-72 w-72 text-slate-950 opacity-[0.05] md:right-12 md:top-12"
    viewBox="0 0 220 220"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path d="M109 188C109 133 109 76 109 32" stroke="currentColor" stroke-width="3" stroke-linecap="round"></path>
    <path d="M109 82C86 58 64 47 42 49C45 74 62 91 109 102" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"></path>
    <path d="M110 110C139 78 164 66 190 69C186 98 165 119 110 132" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"></path>
    <path d="M109 145C82 120 58 109 35 113C40 140 61 158 109 166" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"></path>
  </svg>
  <div class="section-shell relative">
    <div class="max-w-3xl">
      <p class="eyebrow">Areas of care</p>
      <h2 class="section-title">Conditions We Treat</h2>
      <p data-section-summary class="section-copy">{conditionsIntro}</p>
    </div>
    <ul
      class="mt-14 grid grid-cols-1 gap-x-12 sm:grid-cols-2 lg:grid-cols-3"
      aria-label="Conditions treated"
    >
      {
        conditions.map((condition) => (
          <li class="border-t border-slate-200 py-5">
            <h3 class="text-lg font-semibold tracking-tight text-slate-950">
              {condition}
            </h3>
          </li>
        ))
      }
    </ul>
  </div>
</section>
